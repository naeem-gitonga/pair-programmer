services:
  llm:
    build:
      context: ./llamacpp
      dockerfile: Dockerfile
    ports:
      - "8004:8004"
    volumes:
      - ./llamacpp/models:/models
    environment:
      ENV: production
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  smolvlm2:
    build:
      context: ./vllm
      dockerfile: Dockerfile.smolvlm2
    ports:
      - "8005:8005"
    volumes:
      - ./vllm/models:/models
    environment:
      - PORT=8005
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
